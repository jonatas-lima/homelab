{{- if .Values.prowlarr.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prowlarr-route
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
    - name: servarr
  hostnames:
    - servarr.uzbunitim.me
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.prowlarr.httpRoute.path }}
      backendRefs:
        - name: {{ include "prowlarr.fullname" . }}
          port: {{ .Values.prowlarr.service.port }}
{{- end }}
